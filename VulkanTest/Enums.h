//-------------------------- ENUMS -------------------------------//

enum QueueFamily {GRAPHICS = 0, PRESENTATION, TRANSFER, COMPUTE};

enum ModelLoadingType { OBJ, glTF, glB };

enum SkyboxFaces { TOP = 0, BOTTOM = 1, LEFT = 2, RIGHT = 3, BACK = 4, FORWARD = 5 };

enum CommandBufferType { GRAPHICS = 0, TRANSFER = 1};

enum TextureType { BASE_COLOR, EMISSIVE, METALLIC_ROUGHNESS, NORMAL, OCCLUSION };

//----------------------------------------------------------------//